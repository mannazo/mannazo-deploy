apiVersion: v1
kind: PersistentVolume
metadata:
  name: mannazu-mysql-pv
  namespace: mannazu
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  nfs:
    path: /mannazu-mysql-data
    server: {{NFS_SERVER_IP}}   # NFS 서버의 IP 주소
